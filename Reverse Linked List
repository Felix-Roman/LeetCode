Here is my C# solution for the Reverse Linked List question on LeetCode!

/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     public int val;
 *     public ListNode next;
 *     public ListNode(int val=0, ListNode next=null) {
 *         this.val = val;
 *         this.next = next;
 *     }
 * }
 */
public class Solution {
    public ListNode ReverseList(ListNode head) {
       ListNode prev = null;
       ListNode curr = head;
       ListNode store;
        
       while(curr != null)
       {
           store = curr.next;
           curr.next = prev;
           prev = curr;
           curr = store;
       }
        
        return prev;
    }
}
